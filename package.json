{
  "scripts": {
    "build": "cd frontend && yarn build && cd .. && wails build",
    "clean": "make clean",
    "dev": "wails dev -debounce 1000 2>&1 | grep -v -e 'AssetHandler|KnownStructures'",
    "install": "cd frontend && yarn install && cd - && go mod tidy && yarn build",
    "lint": "cd frontend && yarn lint && cd .. && golangci-lint run",
    "type-check": "cd frontend && yarn type-check && cd ..",
    "start": "yarn dev",
    "test-go": "go test ./...",
    "test-go-detail": "echo 'detailed testing may take 30 seconds...' ; cd ./pkg/types ; go test -tags=detailed ./... ; cd -",
    "test-dalle": "cd dalle && go test .",
    "test-tsx": "cd frontend && yarn test",
    "test": "export $(grep -v '^#' ./.env | xargs) >/dev/null && yarn test-go && yarn test-tsx",
    "test-detail": "export $(grep -v '^#' ./.env | xargs) >/dev/null && yarn test-go-detail",
    "CONVENTIONS": "echo 'See copilot.md for development conventions'"
  },
  "devDependencies": {
    "react-icons": "^5.5.0"
  }
}
